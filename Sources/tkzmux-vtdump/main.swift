// tkzmux-vtdump — headless VT tooling (record / replay / modes / render / abi / version).
// Implemented in M1.1 (abi, version) and M1.3 (record, replay). See docs/design.md → Testing without UI.
import Foundation
import TkzTerminalCore

FileHandle.standardError.write(Data("tkzmux-vtdump: not implemented yet (TKZ-7 / TKZ-9)\n".utf8))
exit(2)
