# tkzmux — design

> Living document. Started as the approved planning document (2026-09-07); the design sections were moved here in M0.1 (TKZ-5). Spike results from M1.1–M1.3 are written back into the relevant sections. Tickets live in Linear (see *Roadmap*) and point at sections of this file.

## Executive summary

- **What**: a personal cmux replacement. Sidebar of Claude Code sessions grouped by repo, one terminal per session, app-level git/Claude status bar, Thomas' own design (Claude Design project `452d6955…`, theme *2c Midnight indigo*).
- **Stack**: Swift 6.2, AppKit-first, own Metal renderer, **libghostty-vt** (Ghostty's headless VT core, MIT) as the *only* third-party dependency, vendored as a prebuilt xcframework so daily builds are plain `swift build`. No SwiftUI on hot paths, no Sparkle/Sentry/telemetry.
- **Why this split**: the VT state machine is where decades of correctness live (Claude Code alone needs DEC 2026, kitty keyboard, SGR mouse, OSC 8/52, alt screen). The renderer and surface lifecycle are where cmux's cost lives (2.47 GB, 167 threads, 7–9 % idle CPU for 31 panels). Reuse the former, own the latter.
- **Target**: macOS 26+, arm64 only. Replace cmux as daily driver within weeks; sharing with friends later needs no structural change (see *Sharing readiness*).
- **v1 scope**: one surface per session (no splits), two Claude accounts, session restore, plain groups, cmux keybindings.

## Context

Thomas runs 20 cmux workspaces / 31 panels with ~7 live Claude Code sessions across two subscriptions (`~/.claude` "Private", `~/.claude-alt` "Almi"). cmux (Swift + a 30-patch libghostty fork, GPL-3) is noisy, UX-opaque and heavy. He has a finished design: grouped session sidebar with colour-edged groups, "N working · N need you" strip, one Claude Code terminal per session, status bar (branch, WT badge, model badge, diff stats, ahead/behind, ports, context %, usage %). Motivation for owning the renderer: performance on an M4 Max (36 GB, 7680×2160, macOS 26.6, Xcode 26.1, Swift 6.2). Goal: daily driver within weeks; not (yet) public.

## Decisions (all confirmed with Thomas)

| Decision | Choice | Why |
|---|---|---|
| Language / UI | Swift 6.2, AppKit-first, Metal | Only zero-dependency native stack; Rust needs objc2/metal crates just to open a window. |
| Terminal | **libghostty-vt** headless core + **own** Metal renderer, NSView, pty, surface lifecycle | Reuse validated behaviour, own the architecture. Full libghostty embed = per-surface threads + fork pressure (cmux). From-scratch VT = debugging Unicode/reflow/IME inside live work. |
| Tooling | Pure SwiftPM + `make app` (metallib via `xcrun metal`, bundle, codesign). No .xcodeproj | CLI-driven, Claude-Code-friendly. |
| Deployment | **macOS 26+, arm64 only** | Thomas' pick; universal/older is a flag away if ever needed. |
| Theme | **2c Midnight indigo** default; 2a/2b/1a/1b as presets from the same token set | Thomas' pick. |
| Shortcuts | **cmux bindings** (from `~/.config/cmux/cmux.json`), workspace→session, tab→terminal: ⌘N new session (picker), ⌘P go to session (= design's "Search sessions…"), ⇧⌘P command palette, ⌘B sidebar, ⇧⌘R rename session, ⌘W close terminal / ⇧⌘W close session, ⌘1-9 select session, ⇧⌘U jump to needs-you, ⌘I notifications, ⌘, settings, ⌘O open folder, ⇧⌘, reload config; ⌘T/⌘D reserved. User-editable. | Muscle memory. |
| v1 scope | One surface/session, **two accounts**, **session restore**; groups are plain containers | Thomas' pick. Splits, browser pane, scheduled-session detection, telemetry: not v1. |
| Linear | Project + milestone per phase, area labels, one issue = one Claude Code session | Team **Tkzmux** `6421dba9-a6a5-4b6d-9e27-1671bb81dcd6`; statuses Backlog/Todo/In Progress/Done. |
| Repo | `git init ~/dev/tkzmux` + `gh repo create tkzmux --private` (gh = tkz0, repo scope); flips to **public** at the first release (M6.5), because Homebrew fetches cask assets unauthenticated | Thomas' OK (2026-09-07). |
| Identity to programs | `TERM=xterm-ghostty`, `TERM_PROGRAM=ghostty`, ship terminfo | Claude Code gates Shift+Enter (kitty) and sync output on a TERM_PROGRAM allow-list; we literally run Ghostty's VT. |
| Sharing readiness (cheap now) | `make app` takes `SIGN_IDENTITY` (default ad-hoc `-`); `LICENSE` (MIT) from day one; the per-session sidecar (`~/.claude/dash-sessions/<id>.json`) is a **tkzmux-owned contract** that the Claude Dash shim happens to write today, later a `tkzmux-statusline` wrapper; no personal names in code (account labels from config); zsh-only shell integration is a known gap | Friends/coworkers later without redesign. Distribution (M6) = tagged releases on a public `tkz0/tkzmux` + `tkz0/homebrew-tap` cask (`brew install --cask tkz0/tap/tkzmux`), notarized Developer ID build; no in-app updater. |

## Evidence (why the plan is not guesswork)

- **What runs in Thomas' terminals** (last 5000 zsh commands): git 762, claude 452, pnpm 155, dotnet 34, pico 38, more 32; no vim/htop/tmux. Terminal must be perfect for zsh + Claude Code.
- **Claude Code 2.1.263 emits** (binary strings): DEC 2026, alt screen 1049 (fullscreen mode), kitty keyboard **`CSI > 5 u`** = `DISAMBIGUATE | REPORT_ALTERNATES` (bits `1<<0` and `1<<2`; **not** REPORT_ALL, which is `1<<3` = 8) on push and `CSI < u` on pop — *measured from `Tests/TkzTerminalCoreTests/Fixtures/claude-boot.tkzrec`: `CSI > 5 u` ×4, `CSI > 1 u` ×0, so the original planning note was wrong. The practical difference from flags 1 is narrow and worth stating precisely: printable keys still send their text, Enter/Tab/Backspace keep `\r`/`\t`/`\x7f`, key releases stay silent, and Shift+Enter is still `CSI 13;2u`. The only observable change is the `:<shifted>` alternate inside CSI u sequences (`Ctrl+Shift+A` → `ESC[97:65;6u` rather than `ESC[97;6u]`). Escape-coding every printable key would need REPORT_ALL, and reporting releases needs REPORT_EVENTS (`1<<1`) — Claude Code requests neither, so `keyUp` and modifier releases legitimately produce zero bytes in our main use case* — plus **DEC 1004** (focus events, so `ghostty_focus_encode` really is needed in v1) and **DEC 2031** (colour-scheme change notification), neither of which was in the original list; SGR mouse 1000/1006, OSC 8, OSC 52, OSC 0 title (which **tracks the running tool**: `✳ Claude Code` idle → `✳ Echo hi` while working — a free "what is this session doing" signal for the sidebar, no hooks needed), OSC 9 notifications, 24-bit + 256 colour, DECRQM, modifyOtherKeys, bracketed paste. **No OSC 7**, so `pwd` stays unset and the cwd must come from `Pty.foregroundProcess()`.
- **cmux now**: 2.47 GB, 167 threads (Ghostty = renderer + io + io-reader + io-gather per surface), 7–9 % CPU idle, 20 workspaces / 31 panels, 7 live Claude sessions. **This is the baseline M1.10 must beat.**
- **Claude Code publishes live session state**: `~/.claude/sessions/<pid>.json` and `~/.claude-alt/sessions/<pid>.json` = `{pid, sessionId, cwd, startedAt(ms), version, kind: interactive|bg, entrypoint, name, nameSource: auto|derived, status: idle|busy, updatedAt, statusUpdatedAt, messagingSocketPath, bridgeSessionId, parkedJobId?, jobId?}` + sibling `<pid>.<sha>.key` (never read). Rewritten **in place** (same inode). `~/.claude-alt/projects` → symlink to `~/.claude/projects`; `sessions/` dirs are separate.
- **Quota**: only via statusline stdin. Thomas' Claude Dash shim (`~/dev/mac-dash/scripts/statusline-capture.js`, installed copy `~/.claude/dash-statusline-capture.js`, overwritten by `npm run install:app`) writes `~/.claude/dash-usage-<account>.json`; reconcile rules in `~/dev/mac-dash/CLAUDE.md` + `src/data/parsers/quota-parser.ts`. Statusline payload also carries `session_id, session_name, model.display_name, workspace.git_worktree, context_window.used_percentage, pr.*`.
- **Hooks** (code.claude.com/docs/en/hooks): `Notification.notification_type` ∈ permission_prompt | idle_prompt (60 s idle) | elicitation_dialog | agent_needs_input…; `Stop.last_assistant_message`; `SessionStart.source`; `SessionEnd.reason`; hooks from `--settings` **merge** with user hooks; hooks inherit env.
- **CLI**: `-w/--worktree [name]` → `<repo>/.claude/worktrees/<name>` (must run from main checkout); `-r/--resume <id|name>`; `--settings <file|json>`; `--session-id` unused by us (sessionId rotates on /clear, resume, fork).
- **Shell env on this machine**: `/etc/zprofile` `path_helper`, `.zprofile` `brew shellenv`, `.zshrc` ends by sourcing `~/.local/bin/env` (prepends `~/.local/bin`) → a PATH prepend from the pty parent loses; hence the ZDOTDIR wrapper. `~/.local/bin/claude` → `~/.local/share/claude/versions/2.1.263`; a second `claude` in `/opt/homebrew/bin`.
- **Toolchain**: `brew` zig **0.16.0** installed (M0.2 / TKZ-6; = Ghostty main minimum); Xcode 26.1 + `xcrun metal` present (**but Xcode 26 needs a separately downloaded Metal Toolchain — a fresh install fails `make app` with `cannot execute tool 'metal' due to missing Metal Toolchain`; fix with `xcodebuild -downloadComponent MetalToolchain`, done on this machine 2026-09-07. The runtime `device.makeLibrary(source:)` fallback does not need it**); `xterm-ghostty` terminfo already system-wide (cmux ships `Resources/terminfo/78/xterm-ghostty`); **JetBrains Mono 2.304** installed via cask `font-jetbrains-mono`, `CTFontCreateWithName("JetBrainsMono-Regular")` resolves (verified M0.2); Menlo fallback stays the rule for machines without it; gh logged in as tkz0; **libghostty-vt vendored at `82232ec`** (M1.1 / TKZ-7, see *Packaging*).
- **libghostty-vt** (Ghostty `main` 1.3.2-dev; latest tag 1.3.1): full headless terminal (`terminal.h`), render-state API (`render.h`: tri-state dirty, `row_iterator_next_dirty`, per-cell style/graphemes/fg/bg/selected), key + mouse encoders, selection gestures, formatter, `snapshot.h`, `ghostty_type_json()` ABI manifest. API explicitly unstable → **pin a commit**, diff the manifest on upgrade. Official `example/swift-vt-xcframework` does `import GhosttyVt`.
- **Design source**: file `Terminal Main Window.dc.html` (5 artboards, JetBrains Mono, SF system UI). Intent from the design chat: auto-group by repo + worktree parent, colour = group, collapsible, status dot + summary strip + pulse, titles from Claude's own name, palette (also transcript search later), presets per repo with startup command, keyboard-first, native vibrancy.

---

## Terminal engine (TkzTerminalCore / TkzTerminalRender / TkzTerminalView)

### Packaging (chosen: xcframework → `.binaryTarget`; done in M1.1 / TKZ-7)
- **Pinned commit**: `vendor/ghostty-vt/COMMIT` (first pin `82232ec`, Ghostty main 2026-09-07, app 1.3.2-dev, lib version string `0.1.0-dev`). Upgrading = `GHOSTTY_COMMIT=<sha> make vendor`, then diff `vendor/ghostty-vt/abi-types.json` (sorted, pretty `ghostty_type_json()` via `tkzmux-vtdump abi`) and `ghostty.terminfo`; the test `abiManifestMatchesVendoredFile` fails if the binary and the manifest drift. Network only at vendoring time.
- `scripts/build-ghostty-vt.sh` (zig 0.16.x, xcodebuild, tic; ~1 min): shallow-fetches the commit into `$TMPDIR/ghostty-vt-src`, then `zig build -Demit-lib-vt -Demit-xcframework=false -Dtarget=aarch64-macos -Doptimize=ReleaseFast` → `zig-out/lib/libghostty-vt.a` (the SIMD-combined, libSystem-overridden archive) → our own `xcodebuild -create-xcframework -library … -headers <ghostty/**.h + module.modulemap "GhosttyVt", umbrella ghostty/vt.h>` → `vendor/ghostty-vt/ghostty-vt.xcframework` (**arm64-only**, committed). Two ticket assumptions were wrong at this commit and are worked around: (1) Ghostty's own lib-vt xcframework step always lipo's arm64+x86_64 (`-Dxcframework-target` only affects GhosttyKit), which would double the archive; (2) `-Demit-terminfo` installs nothing in lib-vt mode (resources only ship with the app executable), so the script runs an 8-line `zig run` wrapper over `src/terminfo/*.zig` (std-only) to print the terminfo source (`vendor/ghostty-vt/ghostty.terminfo`) and compiles it with `tic -x -o Resources/terminfo` → `78/xterm-ghostty`, `67/ghostty`.
- **Spike results (M1.1)**: zig build 55 s on the M4 Max (first run, includes dependency fetch; a re-run is fully cached and reproduces a byte-identical archive); `libghostty-vt.a` 10.5 MiB arm64, xcframework 11 MB (< 30 MB → committed to git; `.gitattributes` marks only the `.a` `-diff`, so header changes show up in upgrade diffs); **no `-lc++` needed** (0 `std::__1` undefined symbols: vendored simdutf/highway are built no-libcxx); build info: simd=true, kitty-graphics=true, tmux-control-mode=false, ReleaseFast; lib deployment target macOS 13 (we require 26). `swift build` links the static archive with no extra linker settings.
- **Resource layout**: SwiftPM requires resources under the target's `path`, but the repo keeps them at the root. The real directory must live at **`Sources/<Target>/Resources/<X>`** and the repo-root `Resources/<X>` is the **symlink** — *not* the other way round. Verified the hard way (M1.4/M1.5): `.copy(...)` reproduces a symlink verbatim into `.build/<triple>/debug/tkzmux_<Target>.bundle/`, and because `.build/debug` is itself a symlink a relative target resolves one directory too high and dangles, so `Bundle.module` finds nothing under `swift test`/`swift run`. The same applies to individual files inside a copied directory, which is why `Shaders/TkzShaderTypes.h` is a real copy of the canonical header guarded by a byte-equality test rather than a link. `make app` therefore copies with **`cp -RL`** (dereferencing), or the `.app` gets dangling links. Wired: `TkzTerminalCore` → `terminfo`; `TkzTerminalRender` → `Fonts`, `Shaders`. Test fixtures live in `Tests/<T>Tests/Fixtures` (also `.copy`).
- `Package.swift`: `platforms: [.macOS("26.0")]`; targets `GhosttyVt` (binary), `TkzPtyShim` (C), `TkzShaderTypes` (C header-only, shared Swift/Metal structs), `TkzTerminalCore` (depends on `GhosttyVt`), `TkzTerminalRender`, `TkzTerminalView`, `tkzmux` (exe), `tkzmux-vtdump` (exe), tests. Swift access to C: `GhosttyVt+Swift.swift` wraps handles as non-Sendable `final class`es with `deinit` frees (`GhosttyTerminalHandle`, `GhosttyRenderStateHandle`, `GhosttyKeyEncoderHandle`, `GhosttyMouseEncoderHandle`), `GhosttyError(result, operation)`, `GhosttyVtInfo` (version, build options, ABI manifest); `raw` pointers are `package`-visible for FrameBuilder.
- `make app`: `xcrun -sdk macosx metal -c Shaders/Terminal.metal -I Sources/TkzShaderTypes/include -o build/Terminal.air && xcrun metallib … -o tkzmux.app/Contents/Resources/default.metallib`; `swift build -c release`; copy binary, `Resources/terminfo`, `Info.plist`, SwiftPM resource bundles; `codesign --force --sign "$SIGN_IDENTITY"` (default `-`). `swift run`/tests fall back to `device.makeLibrary(source:)` from the `.metal` resource.

### Pty (`TkzPtyShim` C + `Pty.swift`)
- C only in the forked child (no Swift runtime after fork): `openpty(&m,&s,NULL,NULL,&ws)` → `fork()` → child `setsid()`, `ioctl(s, TIOCSCTTY, 0)`, `dup2`, reset signal mask/handlers, `chdir(cwd)`, `execve(path, argv, envp)`; exec failure reported via a `FD_CLOEXEC` pipe. Parent: `O_NONBLOCK|FD_CLOEXEC` on master. (Ghostty's own approach; posix_spawn can't do tty setup.)
- Spawn **zsh directly**: `path=/bin/zsh`, `argv=["-zsh","-l"]` (login semantics without `/usr/bin/login`, so env survives). Env from `TerminalEnvironment.make(sessionID:accountConfigDir:)`: `TERM=xterm-ghostty`, `TERM_PROGRAM=ghostty`, `TERM_PROGRAM_VERSION`, `COLORTERM=truecolor`, `TERMINFO=<bundle>/terminfo`, `LANG`, `ZDOTDIR=<tkzmux>/zsh`, `TKZMUX_ZDOTDIR`, `TKZMUX_USER_ZDOTDIR=$HOME`, `TKZMUX_BIN=<tkzmux>/bin`, `TKZMUX_SOCKET`, `TKZMUX_SESSION_ID`, `CLAUDE_CONFIG_DIR` (non-primary accounts only); remove `TERM_SESSION_ID`, `TERMINFO_DIRS`. **No PATH prepend** — the ZDOTDIR wrapper does it last (see Claude section).
- **Shim ABI 2.** `tkz_pty_spawn` is serialized by a static `pthread_mutex_t`: `fork()` duplicates the whole fd table, so a concurrent spawn between `openpty`/`pipe` and the `FD_CLOEXEC` `fcntl` would leak the *other* spawn's error-pipe write end and that spawn would then never see EOF and hang. Master and both pipe ends are `FD_CLOEXEC` before the fork. A failing `chdir` (stale worktree) does not fail the spawn — the child execs from the inherited cwd. A child that fails to exec is reaped inside the shim, so a failed spawn leaves no zombie and no fd.
- **Exit**: `DispatchSource.makeProcessSource(.exit)` + `waitpid(WNOHANG)`, plus an unconditional reap right after `resume()` — kqueue `NOTE_EXIT` never fires for a process that died between `fork()` and the registration. After reaping, the master is drained to EOF bounded to ~100 ms, so output written just before exit still arrives, but a slave held open by another process cannot stall it. The `WNOHANG` poll timer is a fallback that starts only *after* the pty has hung up: zero idle cost. The master fd is closed in exactly one place, the read source's cancel handler; the write source is always cancelled first.
- **`MAX_INPUT`**: a tty in canonical mode accepts only ~1 KiB per line; larger single lines are silently truncated. `TerminalSession`'s paste and `run(command:)` must chunk on **newlines**, not on byte count.
- API: `TerminalEnvironment.make(sessionID:accountConfigDir:tkzmuxDir:baseEnvironment:home:terminfoDirectory:)` (last three injectable, defaulting to the process env / `HOME` / the bundled terminfo) plus `loginShellSpawn(…) -> PtySpawn`; `bundledTerminfoDirectory` tries `Bundle.module` then `Bundle.main/Contents/Resources/terminfo`. `Pty.init(spawn:ioQueue:onData:onExit:)` takes its callbacks as **init parameters**, not settable properties, so nothing can arrive before there is a sink.
- `Pty`: read via `DispatchSource.makeReadSource(masterFD, queue: ioQueue)` draining ≤4×64 KiB per wakeup (`EIO` = EOF); writes only on `ioQueue`, `EAGAIN` → pending buffer + write source; `resize(TerminalSize{rows, cols, cellWidthPx, cellHeightPx})` → `TIOCSWINSZ` after `ghostty_terminal_resize`; exit via `DispatchSource.makeProcessSource(pid, .exit)` (kqueue) + `waitpid`; `foregroundProcess()` = `tcgetpgrp` + `proc_pidpath` + `PROC_PIDVNODEPATHINFO` cwd (no shell integration needed).

### VT bridge (`TerminalSession`)
- `ghostty_terminal_new(nil, &t, cols, rows)`; options via `ghostty_terminal_set`: `USERDATA`, `WRITE_PTY` (append to IO-thread-owned `pendingPtyOutput` — callbacks run synchronously inside `vt_write` and must not re-enter), `BELL`, `TITLE_CHANGED`, `PWD_CHANGED`, `CLIPBOARD_WRITE` (OSC 52 → NSPasteboard, reply SUCCESS; non-standard → UNSUPPORTED), `CLIPBOARD_READ` (DENIED), `DESKTOP_NOTIFICATION` (OSC 9/777/99 → sidebar badge + UNUserNotification when not visible), `PROGRESS_REPORT` (OSC 9;4), `COLOR_SCHEME` (DARK), `SIZE`, `XTVERSION` (`tkzmux <ver>`), `TERMINFO_NAME` (`xterm-ghostty`), `COLOR_FOREGROUND/BACKGROUND/CURSOR/PALETTE` (from theme; base 16 from theme, rest `ghostty_color_palette_default`), `SCROLLBACK_MAX_BYTES` (24 MiB — the library default is only **10 000 bytes**, verified, ≈557 rows at 120 cols, so this must always be set explicitly; NULL = unlimited), `CONTINUATION_MAX_BYTES` (4 KiB, and it must be set **before the first byte is written** or `ghostty_snapshot_encode_alloc` cannot capture an unfinished sequence), cursor style block/blink, `KITTY_IMAGE_STORAGE_LIMIT` small, `TITLE_REPORT` false, **`MODE_DEFAULT` 2027 on** (grapheme clustering defaults to *off* in the library; Ghostty's own app turns it on, and with it off a ZWJ family like `👨‍👩‍👧` splits into three double-width cell pairs that the renderer would have to shape per cell — see *Spike results (M1.3)* item 5). **`DEVICE_ATTRIBUTES` is deliberately not installed**: libghostty answers DA1 `ESC[?62;22c` and DA2 `ESC[>1;<ver>;0c` by itself (verified); a callback is only needed to advertise a different feature set. Installing `CLIPBOARD_READ` (we do, answering DENIED) also enables Kitty paste events (mode 5522).
- **Threading**: per session a serial `ioQueue` (`.userInteractive`) + a **`Mutex` (`import Synchronization`)** guarding the terminal and encoders. *(Corrected in M1 from `OSAllocatedUnfairLock`, which constrains its state to `Sendable` and therefore cannot hold the non-`Sendable` libghostty handles; `Mutex<State>` is `Sendable` for any `State`, so `TerminalSession` is `Sendable` with no `@unchecked`. Verified by compiling both under `-swift-version 6`.)* IO thread: `lock { ghostty_terminal_vt_write }` → flush pending pty output → publish events (`AsyncStream<TerminalEvent>` consumed on main) → `renderSignal` (`DispatchSourceUserDataOr` on main). Main: only marks `needsUpdate` and un-pauses the display link **if this session is visible**. Render tick: `lock { begin_update; end_update; iterate dirty rows into CPU row caches }`, GPU encode unlocked. Input: `lock { encoder setopt_from_terminal; encode }` → pty. C callbacks receive an `IOContext` as userdata, never the session, so a callback structurally *cannot* re-enter the lock; consumer closures run only after the lock is released. Background sessions: IO loop only, no render state, no display link, `ghostty_terminal_compress(INCREMENTAL)` from an idle timer (under the lock) — **measured to pay** (M1.10: 577 → 26 MiB footprint for 113 ms across 30 sessions). Schedule with `IdleCompressionPolicy`, drive the idle delay off `ghostty_terminal_compression_activity`, skip visible sessions, and **take the `.ghsnap` snapshot before compressing**, because reading history back rehydrates it.
- **DEC 2026**: libghostty-vt does **not** hide unsynchronized frames; Ghostty's renderer skips frames while mode 2026 is set and its IO thread resets it after 1 s. tkzmux: `ghostty_terminal_get(DATA_MODE, {mode: ghostty_mode_new(2026,false)})` per tick; active < 1 s → skip `begin_update`, keep link running; ≥ 1 s → force the mode off and render. **Verified (M1.3)**: with 2026 set, a write still leaves the render state `DIRTY_PARTIAL` and the formatter shows the new content — hiding frames is entirely the host's job. `DECRQM ?2026$p` answers `ESC[?2026;1$y` when set / `;2$y` when reset.
- **Snapshot**: `ghostty_snapshot_encode_alloc` under lock → `~/Library/Application Support/tkzmux/sessions/<id>.ghsnap` (lower `SCROLLBACK_MAX_BYTES` first to bound size); restore `ghostty_snapshot_decoder_new_buf` → `decoder_ready(&terminal)` (new terminal, renderable immediately) → re-apply options → decode history pages → spawn a fresh shell (old content + new prompt).

### Metal renderer (`TkzTerminalRender`)
- `FontSet` (CoreText only): **JetBrains Mono is bundled** (`Resources/Fonts/JetBrainsMono-{Regular,Bold,Italic,BoldItalic}.ttf` + `OFL.txt`; SIL OFL 1.1 permits redistribution inside an app carrying its license). `Info.plist` `ATSApplicationFontsPath = Fonts` registers them process-scoped at launch inside the `.app`; outside a bundle (`swift run`, tests) `FontSet` registers the same files from `Bundle.module` via `CTFontManagerRegisterFontsForURL(…, .process, …)` before resolving. The app-scoped copy wins over a differently-versioned system install, keeping cell metrics identical across machines. Menlo fallback; `CTFontCreateForString` per-scalar fallback cache; colour fonts via `kCTFontTraitColorGlyphs`. `CellMetrics` = max ASCII advance × round(ascent+descent+leading), underline/strike from CTFont, integer device px, rebuilt on backing-scale change.
- `GlyphRasterizer`: `CGBitmapContext` (8-bit alpha for grayscale, BGRA premultiplied for colour), `CTFontDrawGlyphs`, synthetic bold via fillStroke; wide graphemes get the 2-cell box. `GraphemeShaper`: single scalar → `CTFontGetGlyphsForCharacters`; multi-scalar (ZWJ, VS16, combining) → `CTLine`/`CTRun`, cached. **No ligatures in v1.**
- `GlyphAtlas`: `r8Unorm` 2048² + `bgra8Unorm` 1024²→2048², shelf packer, batched `replace(region:)`, shared across sessions.
- `TerminalSurface` (only for the **visible** session): `ghostty_render_state_new`, row iterator, per-row caches of `TkzGlyphInstance`/`TkzRectInstance` + bg cell grid. Per tick: `begin_update/end_update`, `get(DIRTY)` (FULL → re-read COLS/ROWS/COLORS/CURSOR), `row_iterator_next_dirty` → `row_get(SELECTION)`, `row_cells_get_multi(RAW, STYLE, GRAPHEMES_LEN, HAS_STYLING)` — **`FG_COLOR`/`BG_COLOR` cannot be batched** (verified M1.5): they signal "unset" with `GHOSTTY_INVALID_VALUE` as the *result code*, and `row_cells_get_multi` stops at the first error, so a batch containing them aborts for almost every cell on screen; the two colours are fetched with individual `row_cells_get` calls, `ghostty_cell_get(WIDE / HAS_HYPERLINK)`; `BG_COLOR` returns `GHOSTTY_INVALID_VALUE` when unset → theme bg; bold-bright is app-side (verified not applied by the lib); then `render_state_clean`. **Cursor and selection are not free overlays** (verified M1.3, corrects the original assumption): a cursor move alone leaves `DIRTY_PARTIAL` with exactly the old and the new cursor row dirty — cheap, so the cursor may still be drawn as an overlay; but setting `OPT_SELECTION` leaves `DIRTY_FULL` and every row comes back from `row_iterator_next_dirty`, so a selection change costs a full row rebuild and the renderer cannot opt out. `FG_COLOR`/`BG_COLOR` do **not** resolve the inverse attribute (both return `GHOSTTY_INVALID_VALUE` for `ESC[7m` on an otherwise unstyled cell; `style.inverse` is true) and do not apply bold-brightening — both are app-side, and when both colours are unset the *defaults* are what get swapped. **SPACER_TAIL cells** (`WIDE == 2`) always report `GRAPHEMES_LEN == 0` and empty UTF-8 — the renderer must skip them, not draw a space. **Sized-struct ABI**: every libghostty struct with a `.size` field uses `MemoryLayout<T>.stride` (see spike row 15).
- **Per-cell resolution order and overlays (M1.5)**: fg = `FG_COLOR` else the render state's default foreground; bg = `BG_COLOR` else *nothing* (`TkzBgCell.color == 0`, which the shader composites to the theme background). Bold-brightening reads the palette index off `style.fg_color` and remaps 0–7 → 8–15; **inverse swaps afterwards**, so an unstyled `ESC[7m` cell swaps the defaults; `faint` drops the glyph alpha; `invisible` drops the glyph. The **selection is composited into the `TkzBgCell`**, never drawn as a rect — a selection change forces `DIRTY_FULL` anyway, so there is nothing to save. The **cursor is the one true overlay**: one `TkzRectInstance` built at flatten time (SOLID below the glyphs, HOLLOW above when unfocused), with `TKZ_GLYPH_FLAG_UNDER_CURSOR` stamped onto the covered glyph during flatten — so a blink or focus change costs one flatten and **no row rebuild**. Underline and strike coalesce into **per-run** rects so dotted/dashed patterns do not restart at each cell boundary. An atlas **rebuild** (not a regrow) invalidates every cached `atlasPos`, so the surface stamps the atlases' `rebuildGeneration` and forces a `DIRTY_FULL` second pass if it moves during a tick. A **backing-scale change means a new `FontSet` → new `GlyphCache` → new `TerminalRenderer`, and every surface must be re-attached.**
- Shaders (`TkzShaderTypes.h` shared structs `TkzUniforms`, `TkzBgCell`, `TkzGlyphInstance`, `TkzRectInstance`): bg full-screen pass sampling the cell grid; instanced rects (fill/hollow/underline single·double·curly·dotted·dashed/strike, procedural); instanced glyphs (gray = `rgb, a*tex.r`; colour = premultiplied). Order: bg → rects-below (block cursor, selection) → glyphs → rects-above (one rect pipeline, drawn twice). Entry points `tkz_{bg,rect,glyph}_{vertex,fragment}`; uniforms at buffer 0, instances at buffer 1, grayscale atlas at texture 0 and colour atlas at texture 1 (both always bound; one glyph pipeline branching on `TKZ_GLYPH_FLAG_COLOR`, since the branch is uniform per quad). Pixel space is device px, origin **top-left, +y down**; atlas coordinates are **texels** (`coord::pixel` sampler), so a 1024→2048 regrow cannot desync already-built instances. Instance colours are packed `uint32` **straight** sRGB (`r | g<<8 | b<<16 | a<<24`); every fragment function returns **premultiplied**. A `TkzBgCell` with alpha 0 means "no explicit background" and composites to the theme default — that is where `GHOSTTY_INVALID_VALUE` from `BG_COLOR` maps. `TkzShaderTypes.h` uses a classic include guard (never `#pragma once`) and `Terminal.metal` wraps its `#include` in `#if !defined(TKZ_SHADER_TYPES_H)`, because `device.makeLibrary(source:)` has no include search path and the loader concatenates the header text in front of the shader text. Layout is pinned by `_Static_assert`s that the C *and* both Metal compilers evaluate, mirrored by `MemoryLayout` assertions in the tests, so Swift, C and Metal cannot silently diverge. `.bgra8Unorm`, opaque layer, sRGB, 3-deep shared `MTLBuffer` ring + semaphore. **Dirty FALSE + no overlay change → no drawable acquired** (idle = zero GPU work).

### View & input (`TkzTerminalView`)
- The display-link decision is a **pure `DisplayLinkPolicy`** over a `DisplayLinkDemand` value (`needsUpdate`, `isDragging`, `hasSyncDeadline`, `isLiveResizing`, `isOccluded`, `hasVisibleSession`), so it unit-tests without a window; `DisplayLinkDriver` only owns the `CADisplayLink` and logs each pause/resume. **The cursor blink is an event, not a demand** — a 530 ms timer flips `TerminalSurface.cursorBlinkOn`, which marks the surface dirty and wakes the link for exactly one frame; modelling it as a demand would hold the link at 120 Hz forever. `show(session)` clears the *outgoing* session's `renderSignal` before installing one on the incoming session, so a background session structurally cannot wake the link (asserted by test: 50 writes to a background session produce zero signals and zero resumes).
- A backing-scale change goes through `TerminalRenderContext` (the app-wide `FontSet` → `GlyphCache` → `TerminalRenderer` owner plus a weak registry of live surfaces): new font set, new atlases, new renderer, **re-attach every registered surface**, then re-derive the grid — new metrics fit a different number of cells. A drawable-size change with an unchanged grid still marks the surface dirty, or Core Animation stretches the previous texture over the new bounds.
- **`presentsWithTransaction`**: `MTLCommandBuffer.present` schedules an asynchronous present *outside* the Core Animation transaction, which is the tearing the live-resize path exists to avoid. `TerminalRenderer.render(surface:layer:)` therefore reads `layer.presentsWithTransaction` itself and does the `waitUntilScheduled()` + `drawable.present()` handshake internally, acquiring the drawable **after** the skip check so the idle guarantee holds on the resize path too.
- `RenderSignalRelay` coalesces a session's IO-queue `renderSignal` into at most one in-flight main-thread hop (a busy `cat` signals thousands of times per frame otherwise). `CADisplayLink` retains its target, so a terminal view must be removed from its window before being dropped.
- The cursor on a **SPACER_TAIL** cell spans both cells with its origin shifted one column left, or the block sits on the right half of a CJK character and hides nothing that is there.
- `TerminalMetalView: NSView, NSTextInputClient`; `makeBackingLayer → CAMetalLayer` (`displaySyncEnabled`, `maximumDrawableCount = 2`, `framebufferOnly`); `NSView.displayLink(target:selector:)` paused unless `needsUpdate` / blink / drag / 2026 deadline; `preferredFrameRateRange` 60…120; occlusion pauses; `presentsWithTransaction` only during live resize (commit → `waitUntilScheduled` → `present`). `show(session)` detaches the previous `TerminalSurface` and attaches a new one (FULL rebuild); one `TerminalRenderer` (pipelines, atlases, buffers) app-wide.
- **Keyboard**: IME first (`inputContext.handleEvent`; marked text = preedit overlay), then a `KeyPress` (`TkzTerminalCore/KeyEncoder.swift` — deliberately in **Core, not View**, so the whole encoding path unit-tests without AppKit): action, physical key via `MacKeyCodes.swift` (a from-scratch macOS `kVK_*` → `GhosttyKey` table cross-checked against Ghostty's `keycodes.zig`, no AppKit import), mods incl. side bits from the `NX_DEVICER*KEYMASK` raw flags, utf8 filtered of C0/DEL/PUA U+F700–F8FF, unshifted codepoint from `characters(byApplyingModifiers: [])`, consumed mods, composing → `lock { ghostty_key_encoder_setopt_from_terminal; re-set MACOS_OPTION_AS_ALT (setopt_from_terminal resets it — documented in key/encoder.h); encode }` → pty. **Option-as-alt needs the view's help** (verified M1.7): libghostty computes `effectiveMods = mods − consumedMods` whenever text is non-empty, so building the press the obvious way (text `∫`, Option consumed) encodes `∫` even with `MACOS_OPTION_AS_ALT` set. When Option acts as Alt the view must translate the key *without* Option (text `"b"`, Option left **unconsumed**) — Ghostty does this via `ghostty_surface_key_translation_mods`, which libghostty-vt does not export, so `KeyEncoder.translationModifiers(for:optionAsAlt:)` reimplements it. The full encoding matrix is **generated** into `docs/keys.md` by the tests and asserted on every run, so it cannot drift. `keyUp` → RELEASE, `flagsChanged` → modifier press/release, `performKeyEquivalent` lets ⌘-shortcuts through to the app, `doCommand(by:)` no-op. Non-keyDown text (emoji picker, dictation) → `ghostty_terminal_paste(source: TEXT)`.
- **Mouse**: tracking area; if `DATA_MOUSE_TRACKING` and no Shift → `GhosttyMouseEvent` → `ghostty_mouse_encoder_encode` (options synced via `setopt_from_terminal`, size via `OPT_SIZE`); else selection via `GhosttySelectionGesture` (PRESS/DRAG/RELEASE, CELL/WORD/LINE behaviours, Option = rectangle, autoscroll ticks) → `ghostty_terminal_set(OPT_SELECTION)`. Copy = `ghostty_terminal_selection_format_alloc` (PLAIN, unwrap, trim). Wheel: tracking on → one *press* report per row, button four (up) / five (down) — measured SGR bytes `ESC[<64;col;rowM` up, `ESC[<65;col;rowM` down, and a wheel notch sends **only the press**; off → `ghostty_terminal_scroll_viewport(DELTA)` (up is negative), with fractional trackpad deltas accumulated into whole rows by `ScrollAccumulator`. `setopt_from_terminal` syncs only tracking mode and format — `OPT_SIZE` and `OPT_ANY_BUTTON_PRESSED` are the embedder's, so `MouseEncoder` tracks held buttons itself and a motion event must carry the **held** button or drags are silent under mode 1002. A drag includes the cell under the pointer only past its horizontal midpoint, so the view must pass **real pointer pixels**, never a re-centred cell coordinate. Paste = `ghostty_terminal_paste` (bracketed + chunking inside the lib) after `ghostty_paste_is_safe` (confirm sheet if unsafe). OSC 8: hover with ⌘ → `ghostty_terminal_grid_ref` + `ghostty_grid_ref_hyperlink_uri` → pointing-hand + underline overlay, ⌘-click opens. Focus in/out → `ghostty_focus_encode` when mode 1004; hollow cursor when unfocused.

### TerminalHost — the seam between halves (protocol in `TkzApp`)
```swift
protocol TerminalHost: AnyObject {
  func open(_ id: SessionID, cwd: String, env: [String: String], size: TerminalSize) throws -> pid_t   // shell pid
  func run(_ id: SessionID, command: String)          // types command + "\r"
  func show(_ id: SessionID?)                          // attach the single renderer; nil = none
  func resize(_ id: SessionID, _ size: TerminalSize)
  func close(_ id: SessionID, signal: Int32)           // SIGHUP default; row stays resumable
  func snapshot(_ id: SessionID) throws -> Data        // .ghsnap
  func restore(_ id: SessionID, from: Data, cwd: String, env: [String: String]) throws -> pid_t
  var events: AsyncStream<(SessionID, TerminalEvent)> { get }
    // .title(String) .pwd(String) .bell .notification(title, body) .progress(state, value) .exited(ExitStatus) .foreground(pgid, path?, cwd?)
}
```

### Spike results (M1.1–M1.3)
All C calls stay confined to `GhosttyVt+Swift.swift`, `KeyEncoder.swift`, `MouseEncoder.swift`, `SelectionController.swift`, `PasteSupport.swift`, `FrameBuilder.swift`. Everything below was measured against the pinned commit `82232ec`, not inferred; anything still open says so.

| # | Question | Answer |
|---|---|---|
| 1 | c++ link needed? | **No** (M1.1). 0 `std::__1` undefined symbols; vendored simdutf/highway build without libc++. |
| 2 | Does render-state row iteration need the lock? | **No — only `begin_update` does.** `render.h` 42–51 documents the two-phase split; confirmed by mutating the terminal between `begin_update` and `end_update`: iteration returned the pre-`begin` content, no crash, no corruption. Row/cell data is stale-but-valid until the next update. ⇒ lock only around `begin_update`; GPU encode unlocked. |
| 3 | 2026 dirty semantics | The library **does not** suppress unsynchronized frames: with 2026 set, a write still leaves `DIRTY_PARTIAL`. `DECRQM ?2026$p` → `ESC[?2026;1$y` set / `;2$y` reset. Skipping is the host's job (`SyncOutputWatchdog`). |
| 4 | `FG_COLOR` inverse handling | **App-side.** `ESC[7mX` → both colours `GHOSTTY_INVALID_VALUE`, `style.inverse` true. Bold-brightening is also app-side: `ESC[1;31mZ` → palette[1], not bright palette[9]. |
| 5 | Cursor / selection dirtiness | Cursor move → `DIRTY_PARTIAL`, exactly the old + new cursor row. **Selection set → `DIRTY_FULL`, every row re-iterated** — a selection change is a full rebuild, not an overlay. |
| 6 | WIDE / SPACER_TAIL on CJK + emoji | `你`, `😀` → lead cell `WIDE=1` + tail `WIDE=2` (`GRAPHEMES_LEN=0`, empty UTF-8). **Mode 2027 defaults to *off***, which splits `👨‍👩‍👧` into three double-width pairs (6 cells); with 2027 on it is one pair (2 cells, `graphemes=5`). ⇒ tkzmux sets `MODE_DEFAULT` 2027 on, matching Ghostty. |
| 7 | `BG_COLOR` when unset | `GHOSTTY_INVALID_VALUE` (-2), *not* `GHOSTTY_NO_VALUE` (-4). Substitute the theme colour. |
| 8 | Hyperlink-ref cost while scrolled | **Free.** ~7 ns/lookup via `grid_ref(VIEWPORT)`, ~8 ns via `POINT_TAG_HISTORY`, with 20 000 lines of scrollback (200 k iterations, -O2 C). No caching needed for ⌘-hover. |
| 9 | Kitty flags + DA replies | After `ESC[>1u`: flags = 1 (`DISAMBIGUATE`). **Real Claude Code pushes `ESC[>5u` = `DISAMBIGUATE\|REPORT_ALTERNATES`** — verified in the committed fixture (4 occurrences, zero of `>1u`) and against `key/encoder.h` bit values. Encodings are identical to flags 1 except for `:<shifted>` alternates in CSI u sequences; releases would need `REPORT_EVENTS` (2), which Claude Code does not set. `ESC[?u` → `ESC[?1u`. DA1 → `ESC[?62;22c` and DA2 → `ESC[>1;10;0c` **with no callback installed**; a `DEVICE_ATTRIBUTES` callback fully overrides, so we install none. |
| 10 | Wheel button 4/5 bytes (SGR) | `64 + (button − 4)`: up `ESC[<64;c;rM`, down `ESC[<65;…`, left `66`, right `67`; release is the `m` form. Real terminals send **only the press** for a wheel notch — the view encodes PRESS and drops RELEASE. |
| 11 | `GhosttyMimeReader` contract | `GhosttyPaste` (sized) carries `mimes` (borrowed, preference order), `reader {read, userdata}`, `location`, `source` (CLIPBOARD for a user paste, TEXT for IME commit / drag-drop — TEXT never becomes a paste event) and `allow_unsafe`. `read` is called **at most once per paste**, only for the representation actually pasted; the `mime` handed back is the same pointer as the `mimes` entry, so it can be identified by pointer. It streams via `writer.write` in pieces of any size, returns true only when fully written; nothing written is retained. Output reaches WRITE_PTY in chunks that must hit the pty in order. `GHOSTTY_REJECTED` = unsafe, nothing written → confirm, retry with `allow_unsafe`. |
| 12 | Default scrollback + RSS per idle session | **Default `SCROLLBACK_MAX_BYTES` is 10 000 bytes** (≈557 rows at 120 cols) — the 24 MiB option is a *raise*, not a tightening, and a session that forgets it has almost no scrollback. One 120×40 session: **+32 KiB idle**; filled with 20 000 coloured lines: **+19.0 MiB / 19 961 rows**. No shared overhead across 20 sessions. 7 live full-scrollback sessions ≈ 136 MiB vs the 2.47 GB cmux baseline. |
| 13 | `compress(FULL)` / `compress(INCREMENTAL)` | **Reversed in M1.10 — the M1.3 negative was a metric artefact.** The spike measured `resident_size`, and on Darwin pages released with `MADV_FREE` stay resident until the kernel needs them, moving into `task_vm_info.reusable`. Re-measured on **30 live pty sessions** (19 963 rows each, release, with a `--no-compress` control): looping `compress(INCREMENTAL)` to `COMPLETE` cost 1 710 steps / **113 ms** and moved `phys_footprint` **577 → 26 MiB** while `reusable` went **0 → 598 MiB**; the control held at 577 → 577 MiB and row counts were identical in both. `resident_size` *rises* (583 → 630 MiB), which is what fooled the spike. ⇒ **compression pays; measure `phys_footprint`, never RSS.** Reading history back rehydrates it, so **snapshot before compressing**. ~66 µs per step, ~3.8 ms to take one 20 000-row session to `COMPLETE`. See `docs/perf.md`. |
| 14 | Snapshot size / restore time | 120×40 with 20 000 lines: encode **4.33 MiB in 1.5 ms**; full decode **4.7 ms**. Incremental: `decoder_ready` gives a renderable terminal in **0.1 ms**, history streams after. ⇒ restore-on-launch shows a usable screen almost instantly. **Snapshot size is *content*-based while live memory is *page*-based** (M1.10): 20 000 rows cost ~22.4 MiB live for either corpus, but the snapshot is 7.69 MiB for uniform 100-column colour vs **0.96 MiB** for real `ls -laR` output — 36 % vs 4.3 % of live RSS. The "≈19 %" figure is one corpus, not a constant. |
| 15 | Sized-struct ABI | Every libghostty struct with a leading `.size` field must be initialised with **`MemoryLayout<T>.stride`** (`GHOSTTY_INIT_SIZED` is a macro and does not import into Swift). Re-verified directly: for all ten sized structs tkzmux uses, Swift `.size` == `.stride` == C `sizeof`, so `.size` happens to work today — but `.stride` is the rule because it can never be smaller than C's `sizeof`, whereas `.size` omits trailing padding and would silently under-report if a future upgrade adds a padded field. |
| 16 | Snapshot decoder lifetime | `ghostty_snapshot_decoder_new_buf` **borrows** the bytes — the whole decode must happen inside one `Data.withUnsafeBytes`. |
| 17 | Title/pwd read timing | `DATA_TITLE`/`DATA_PWD` are readable **inside** their callbacks; reading after `vt_write` coalesces two OSC 0s in one write into one event. |
| 18 | pty job control, display-link pause, `presentsWithTransaction`, `Bundle.module` in a hand-built .app, Menlo/emoji fallback, ABI-manifest diff | Covered by M1.2 / M1.4 / M1.6 / M1.9 — see those sections.

### Testing without UI
`.tkzrec` v1 = one line of JSON (`magic`, `version`, `cols`, `rows`, `argv`, `env` summary, `startedAt`, `note`) followed by binary frames `[u8 kind][u64 LE elapsedNanos][u32 LE len][payload]`; kind 0 = pty output, 1 = resize (u16 cols, u16 rows), 2 = host input. Unknown kinds are skipped by length and a truncated final frame is tolerated (a recording ends when the session is killed). `head -1 f | jq` inspects it; writer/reader in `TkzTerminalCore/Recording.swift`. `tkzmux-vtdump record --out X.tkzrec [--cols N --rows M] [--cwd D] [--script F] [--golden G] [--note T] [--timeout MS] [--quiet] [--inherit-env] -- <cmd>` spawns `<cmd>` on a pty under the full `TerminalEnvironment` and tees every byte in. The child gets a **minimal** base env (`HOME`, `PATH`, `SHELL`, `USER`, `LOGNAME`, `TMPDIR`, `LANG`) plus the tkzmux contract, so a recording is reproducible and cannot carry the recorder's environment (`--inherit-env` opts out); the header stores only the five VT-relevant variables, asserted by a test. A `--script` file drives the child: `wait <ms>`, `waitfor "<text>" [<ms>]` (polls the rendered screen), `send "<text>"` (escapes decoded, chunked at newlines because a canonical-mode tty truncates past `MAX_INPUT`), `resize <cols> <rows>`, and `stop` — **SIGKILL, deliberately**: ending with SIGHUP lets Claude Code tear down cleanly and the fixture then records every mode being *reset*, which is a correct recording of a useless fact. Fixtures carry a `README.md` recording provenance and sanitization. `vttest-menu1` is absent: `vttest` is not installed and was deliberately not installed. Original spelling for reference: `tkzmux-vtdump record --cols 120 --rows 40 --out claude-boot.tkzrec -- claude` (tees raw pty bytes + timestamps under the tkzmux env so TERM_PROGRAM gating is real); `replay --format plain|vt|html` via `ghostty_formatter_format_alloc`; `--modes` prints 1049/2004/1000/1006/2026/25/1004 + kitty flags; `render --png` offscreen; `abi`; `version`. Fixtures: `zsh-ls-color`, `claude-boot`, `claude-tool-run` with golden screens (`vttest-menu1` absent — vttest not installed). **`claude-boot` and `claude-tool-run` end with mode ?1049 (alt screen) set and 0 scrollback rows**: Claude Code's TUI lives in the alt screen, so replaying them any number of times builds no history — they are golden-screen fixtures, not a scrollback corpus.  encoder tests (Shift+Enter with/without kitty, Option+B, SGR press); pty tests (`stty size`, `tty`, exit code, resize, no zombies); render-state tests (styles, wide, hyperlink, dirty semantics); render tests (metrics, fallback, atlas regrow, PNG hash). Dev-time conformance: `brew install vttest` menus 1/2/3/6/11 and esctest subsets, results in `docs/conformance.md`.

---

## App architecture (TkzCore / TkzApp)

```
Package.swift · Makefile · LICENSE (MIT) · CLAUDE.md · docs/design.md (this document) · docs/conformance.md
docs/  design.md · keys.md (generated) · conformance.md · perf.md
scripts/  build-ghostty-vt.sh · make-app.sh · record-vt.sh
vendor/ghostty-vt/  COMMIT · LICENSE · abi-types.json · ghostty.terminfo · ghostty-vt.xcframework/ (arm64 static, committed)
Resources/  Info.plist · terminfo/ · shim/claude.sh · zsh/{.zshenv,.zprofile,.zshrc,.zlogin} · Shaders/Terminal.metal
Sources/
  TkzPtyShim (C) · TkzShaderTypes (C)
  TkzTerminalCore · TkzTerminalRender · TkzTerminalView          (see Terminal engine)
  TkzCore        models + pure reducers: Group, Session, SessionStatus, Account, GitSummary, UsageSnapshot,
                 ClaudeSessionInfo, Preset, HookEvent; AppState/AppStore/ChangeSet; StatusDerivation; Theme tokens
  ClaudeBridge   ClaudeSessionWatcher · HookServer · ShimInstaller · SettingsMerge · UsageReader · SessionSidecarReader
  GitStatus      GitStatusService · RepoInfo · FSEventsWatcher · PRLookup · PortScanner (libproc)
  Persistence    StateFile (state.json v1, atomic, .bak, migrations) · Snapshots
  TkzApp         AppDelegate · MainWindowController · Sidebar/ · StatusBar/ · Palette/ · Toolbar/ · TerminalHost (impl over TkzTerminalView)
  tkzmux (exe) · tkzmux-vtdump (exe) · tkzmux-hook (exe, `import Darwin` only)
Tests/  TkzCoreTests · TkzTerminalCoreTests · TkzTerminalRenderTests · ClaudeBridgeTests · GitStatusTests · PersistenceTests
```

- **Store**: `AppStore` (`@MainActor`) owns `AppState` (groups, sessions, accounts, usage, selection, sidebarVisible, windowFrame, presets, shortcuts). `store.update { … }` diffs into `ChangeSet {sessions, groups, structure, selection, usage}` delivered once per run-loop turn. Services run on their own queues/actors and post into the store; they never touch views. No `@Observable` (row-granular reloads need explicit change sets).
- **Sidebar** = view-based `NSOutlineView`, fixed heights (group 28 pt, session 44 pt), items keyed by IDs; per-row `reloadData(forRowIndexes:)`, structural `insert/remove/moveItem`. Group header: uppercase name, 2–3 pt colour edge (`CALayer`), chevron, ＋. Row: status dot, title, `⎇ branch` + `WT` (JetBrains Mono 10 pt), `NEEDS YOU` amber badge, account chip. **Pulse** = one `CABasicAnimation` on a layer (GPU-side, zero app CPU), only on visible `working` rows, paused when occluded. Summary strip “5 working · 2 need you”.
- **Status bar**: `⎇ branch` · `WT` · model badge · `+142 −38 · 12 files` · `↑0 ↓2` · ports · `Context 62%` · `Usage 5% · resets 4d 12h`; re-renders only when the selected session's ChangeSet intersects.
- **Toolbar**: title “<session> — <group>”; `NSMenuToolbarItem` “＋ New session…” scoped to the selected group (*New worktree (claude -w)*, *In repo root (claude)*, *In another repo…*, *From preset… (n saved)*, Account submenu); “Search sessions…” (⌘P); the four right-hand buttons from the design (`>_` new terminal, `◍` browser, `◫`/`⬓` splits — last three disabled in v1).
- **Palette** (⇧⌘P; `NSPanel` + `NSVisualEffectView`): fuzzy over sessions (title, branch, cwd, group), groups, commands, presets.
- **Theme** (`TkzCore/Theme.swift`, M0.2 / TKZ-6): one `struct Theme` with the tokens below; the five artboards are `static let` presets of the same struct (`Theme.default = .midnightIndigo`, `Theme.allPresets`). `RGB` (`TkzCore/RGB.swift`) is sRGB + straight alpha with `hexString`, `mixed(with:amount:)`, `over(_:)`, WCAG `relativeLuminance` / `contrastRatio(against:)`. No AppKit in TkzCore: NSColor conversion belongs to the consumer. `terminalForeground` is one token beyond the ticket's list because every artboard draws terminal text dimmer than UI titles and libghostty's `COLOR_FOREGROUND` needs the terminal one. Fonts are the same in every preset: UI = system font 12.5 (title) / 11 (body) / 10.5 (caption) pt; mono = JetBrains Mono (`JetBrainsMono-Regular`, fallback Menlo) 12.5 (terminal) / 10 (sidebar branch line) / 10.5 (status bar) pt. Extraction rules per token are in the source comments; the table below is generated by `ThemeTests.printsDesignTable` (`swift test --filter ThemeTests/printsDesignTable`) — regenerate it after any token change.

  **Terminal palette rule** (ANSI 0–15; 16–255 = `ghostty_color_palette_default`): mapped by artboard *swatch*, not by token (2b's accent is coral). 1 red = diffRemove, 2 green = working, 3 yellow = waiting, 4 blue = the "● Update(…)" tool dot, 5 magenta = the "● Bash(…)" dot, 6 cyan = the teal of the plan-mode line / group edge. Dark presets: 0 = statusBarBackground, 7 = terminalForeground, 8 = foregroundDim, 15 = foreground, bright 9–14 = base mixed 25 % toward white. Light: 0 = foreground, 7 = foregroundMuted, 8 = foregroundDim, 15 = terminalForeground, bright 9–14 = base mixed 15 % toward black (never toward the white background). The Claude orange has no ANSI slot. Contrast (tested, all five presets): foreground and terminalForeground ≥ 7:1 on terminalBackground, foregroundMuted ≥ 4.5:1, chromatic slots ≥ 3:1 (dark) / ≥ 2.5:1 (light — yellow on white is 2.7:1).

  | Token | 2c Midnight indigo (default) | 2a Graphite | 2b Warm charcoal | 1a Dark | 1b Light |
  |---|---|---|---|---|---|
  | windowBackground | #141624 | #141519 | #191512 | #1b1d21 | #f5f5f7 |
  | titlebar | rgba(31,34,54,.95) | rgba(30,32,37,.95) | rgba(40,33,28,.95) | rgba(38,40,45,.92) | rgba(246,246,248,.95) |
  | sidebarBackground | #1a1d31 | #1b1d22 | #201b17 | rgba(32,34,38,.96) | rgba(236,238,241,.96) |
  | terminalBackground | #0e101c | #0f1013 | #141110 | #17181b | #ffffff |
  | statusBarBackground | #1e2136 | #191b20 | #241e19 | #1f2125 | #eff0f3 |
  | foreground | #edf0fd | #f5f7fa | #f4eee7 | #f4f6f9 | #26292e |
  | terminalForeground | #e2e6f8 | #e3e8f0 | #e9e2da | #dde1e8 | #3a3e45 |
  | foregroundMuted | #98a0c2 | #98a2b3 | #a1968a | #8b93a0 | #6a7280 |
  | foregroundDim | #8890b4 | #8791a0 | #93887c | #8b93a0 | #9ba1aa |
  | accent | #8b93f8 | #5b8def | #e28666 | #4d7fd6 | #4d7fd6 |
  | accentText | #14162a | #ffffff | #2a1c14 | #ffffff | #ffffff |
  | selection | rgba(139,147,248,.22) | rgba(91,141,239,.24) | rgba(226,134,102,.22) | rgba(77,127,214,.20) | rgba(77,127,214,.14) |
  | working | #4ade80 | #3ddc74 | #4cc97e | #34b060 | #2c9e53 |
  | waiting | #fbbf54 | #ffb454 | #ffb454 | #f0a03a | #dd8a1e |
  | idle | rgba(255,255,255,.30) | rgba(255,255,255,.30) | rgba(255,255,255,.30) | rgba(255,255,255,.25) | rgba(0,0,0,.22) |
  | needsYouText | #fbbf54 | #ffb454 | #ffb454 | #f0a03a | #b06e10 |
  | needsYouBackground | rgba(251,191,84,.16) | rgba(255,180,84,.16) | rgba(255,180,84,.16) | rgba(240,160,58,.14) | rgba(221,138,30,.14) |
  | wtText | #c3c8fd | #b9cdf5 | #f0b39c | #8fb0e8 | #3a66b5 |
  | wtBackground | rgba(139,147,248,.20) | rgba(91,141,239,.22) | rgba(226,134,102,.20) | rgba(77,127,214,.16) | rgba(77,127,214,.13) |
  | groupEdgeDefault | #41c6a8 | #3fbf9f | #3fbf9f | #3fa08c | #2c8a74 |
  | diffAdd | #4ade80 | #3ddc74 | #4cc97e | #34b060 | #2c9e53 |
  | diffRemove | #f28b8b | #f07a7a | #e87f7f | #d16a6a | #c04a4a |
  | border | rgba(255,255,255,.08) | rgba(255,255,255,.08) | rgba(255,255,255,.08) | rgba(255,255,255,.06) | rgba(0,0,0,.08) |
  | ansi 0 | #1e2136 | #191b20 | #241e19 | #1f2125 | #26292e |
  | ansi 1 | #f28b8b | #f07a7a | #e87f7f | #d16a6a | #c04a4a |
  | ansi 2 | #4ade80 | #3ddc74 | #4cc97e | #34b060 | #2c9e53 |
  | ansi 3 | #fbbf54 | #ffb454 | #ffb454 | #f0a03a | #dd8a1e |
  | ansi 4 | #8b93f8 | #5b8def | #7aa2e8 | #4d7fd6 | #3a66b5 |
  | ansi 5 | #c084fc | #a58bf0 | #b58fd9 | #9a6fc9 | #8352b8 |
  | ansi 6 | #41c6a8 | #3fbf9f | #3fbf9f | #3fa08c | #2c8a74 |
  | ansi 7 | #e2e6f8 | #e3e8f0 | #e9e2da | #dde1e8 | #6a7280 |
  | ansi 8 | #8890b4 | #8791a0 | #93887c | #8b93a0 | #9ba1aa |
  | ansi 9 | #f5a8a8 | #f49b9b | #ee9f9f | #dd8f8f | #a33f3f |
  | ansi 10 | #77e6a0 | #6ee597 | #79d79e | #67c488 | #258647 |
  | ansi 11 | #fccf7f | #ffc77f | #ffc77f | #f4b86b | #bc751a |
  | ansi 12 | #a8aefa | #84aaf3 | #9bb9ee | #7a9fe0 | #31579a |
  | ansi 13 | #d0a3fd | #bca8f4 | #c8abe3 | #b393d7 | #6f469c |
  | ansi 14 | #71d4be | #6fcfb7 | #6fcfb7 | #6fb8a9 | #257563 |
  | ansi 15 | #edf0fd | #f5f7fa | #f4eee7 | #f4f6f9 | #3a3e45 |

## Claude integration (ClaudeBridge)

- **Discovery**: watch `~/.claude/sessions` and `~/.claude-alt/sessions` with a directory `DispatchSource` (add/remove) **and** a per-file `DispatchSource` (`.write|.extend|.delete|.rename|.attrib`) since descriptors are rewritten in place; 100 ms debounce; torn JSON keeps the previous value; liveness 5 s `kill(pid,0)`; pid-reuse guard `proc_pidinfo(PROC_PIDTBSDINFO).pbi_start_tvsec` vs `startedAt`; show `kind=="interactive"`, attach `bg` to its parent via `parkedJobId==jobId`. Idle cost ≈ 40 syscalls / 5 s.
- **Identity**: the shim `exec`s the real `claude`, so the shim's `$$` **is** the descriptor pid; it sends a `launch` frame `{sid, pid, cwd, config_dir, argv}` first, so `pid → SessionID` is known before `<pid>.json` exists. Fallback: `proc_listchildpids` walk from the shell pid. `--session-id` not used; latest `sessionId` recorded for `--resume`.
- **Status derivation** (first match): not alive / SessionEnd(reason ∉ clear,resume) → `exited`; pending permission_prompt|elicitation → `waiting(.permission)`; agent_needs_input → `waiting(.agentInput)`; `parkedJobId` → `idle` (parked); descriptor `busy` → `working`; Stop newer than `attendedAt` and (idle_prompt or ≥60 s) → `waiting(.doneUnattended)` = NEEDS YOU; Stop <60 s → idle “done”; else `idle`. Pending clears on UserPromptSubmit/Stop/SessionEnd/elicitation_complete/descriptor busy newer than the notification. Degrades to descriptor-only without hooks.
- **Titles**: user rename → descriptor `name` when `nameSource != "derived"` → worktree name → `basename(cwd)`.
- **Shim install** at `~/Library/Application Support/tkzmux/{bin/claude, bin/tkzmux-hook, zsh/…, tkzmux.sock, state.json, sessions/, VERSION}`, idempotent, never touches `~/.claude/settings.json`. **ZDOTDIR wrapper** (the single mechanism; no PATH prepend from the pty): each wrapper sources the user's file with `ZDOTDIR` restored, `.zshrc` ends with `export PATH="$TKZMUX_BIN:$PATH"`, `.zlogin` restores `ZDOTDIR` for nested shells. zsh only.
- **Shim** (`bin/claude`, bash): find real `claude` skipping `$TKZMUX_BIN`; pass through for subcommands, `-p`, `--bare`, `--safe-mode`, `-v`, `-h`, or unset `TKZMUX_SESSION_ID`/`TKZMUX_SOCKET`; strip user `--settings`, deep-merge via `tkzmux-hook settings-merge` (hook arrays concatenated, user first; failure → exec untouched); `tkzmux-hook launch`; `exec real … --settings <merged>`. Injected hooks: SessionStart, SessionEnd (timeout 1), UserPromptSubmit, Stop, Notification (matcher `permission_prompt|idle_prompt|elicitation_dialog|elicitation_url_dialog|elicitation_complete|elicitation_response|agent_needs_input`) → `"<bin>/tkzmux-hook <Event>"`.
- **`tkzmux-hook`** (< 20 ms, `import Darwin`): stdin → single line → `AF_UNIX` connect, 200 ms poll → one NDJSON frame `{"v":1,"type":"hook","event","sid","ppid","ts","payload"}` → exit 0 on every path, never writes stdout. App: POSIX listener + DispatchSource, 256 KiB frame cap, keeps 4 KiB of `last_assistant_message` for UI; attribution `sid` → `payload.session_id` → pid tree.
- **Usage**: `UsageReader` watches `~/.claude/dash-usage-*.json` (key = filename), overlay `dash-accounts.json`; reconcile ported verbatim from `quota-parser.ts` (dead-window guard, high-water keyed on `resets_at`, 10-min confirmation TTL, 14-day ghost) with the mac-dash scenarios as tests. Status bar shows `seven_day` for the selected session's account.
- **Per-session sidecar (tkzmux-owned contract)**: `~/.claude/dash-sessions/<session_id>.json` = `{updated_at, session_id, account_key, context_used_percentage, model{id,display_name}, session_name, workspace{git_worktree,project_dir,repo}, worktree?, pr{number,url,review_state}?, cost}`. Producer today: **edit the mac-dash source** `~/dev/mac-dash/scripts/statusline-capture.js` (add `writeSessionSidecar` after `writeSnapshot`, same throttle/tmp+rename/try-catch) and `npm run install:statusline` (the `~/.claude` copy is generated). Later: a `tkzmux-statusline` wrapper for other users. Join on `descriptor.sessionId`; ignore sidecars > 24 h; housekeeping deletes > 7 d.

## Git integration (GitStatus)

- Repo detection per cwd: `git -C <cwd> rev-parse --show-toplevel --git-dir --git-common-dir --abbrev-ref HEAD`; `repoRoot = parent(common-dir)`; `isWorktree = realpath(git-dir) != realpath(common-dir)`.
- Always `--no-optional-locks` / `GIT_OPTIONAL_LOCKS=0`: `status --porcelain=v2 --branch -z` (branch, upstream, `branch.ab`, changed/untracked) and `diff HEAD --shortstat` (~22 ms on CoreInvest). No auto-fetch (optional 10-min pref, off).
- Triggers: one `FSEventStream` per repoRoot (common-dir + session cwds; ignore `.git/objects`, `node_modules`), 300 ms debounce, ≤1/2 s in bursts; after each Stop hook; on selection if >10 s old. Per-repo serial queue; rows re-render only on `GitSummary` change.
- PR: sidecar `pr.*` first; `gh pr view --json number,state,url,isDraft,reviewDecision` only for github.com origins (CoreInvest = Azure DevOps → never), selected session only, throttled, failures cached 10 min.
- Ports via **libproc**: `proc_listchildpids` from the shell pid → `proc_pidinfo(PROC_PIDLISTFDS)` → `proc_pidfdinfo(PROC_PIDFDSOCKETINFO)` → `SOCKINFO_TCP && TSI_S_LISTEN` → `ntohs(insi_lport)`; selected session on selection, after Stop, 10 s timer. (lsof = 30–70 ms + fork; rejected.)

## Session flows & persistence

Start: create `Session` (persist) → `TerminalHost.open` → `run(command)` → shim `launch` binds pid → descriptor appears → SessionStart confirms `claudeSessionId`.
- **New worktree** (cwd = repoRoot, `claude -w [name]`, optional name sheet; descriptor cwd `<repo>/.claude/worktrees/<name>` sets `worktreePath`; `git worktree list --porcelain` on exit). **Repo root** (`claude`). **Another repo…** (palette: known repos, recents, `NSOpenPanel` → new Group). **Preset** `{name, command, cwdMode: repoRoot|worktree(name?)|fixed(path), accountKey?, env}`. Account submenu → `CLAUDE_CONFIG_DIR`.
- **Restore**: sidebar rebuilt from state.json with rows `exited`; terminal content from `.ghsnap`; “Resume” / “Resume all in group” / auto-resume pref → `claude --resume <claudeSessionId>` in `worktreePath ?? repoRoot ?? cwd` (worktree gone → repoRoot, WT badge cleared).
- **Close** (confirm if working/waiting; SIGHUP; row stays resumable) vs **Remove** (deletes from state; never touches the worktree). **Rename** sets `Session.title` only.
- `state.json` v1 `{schemaVersion, groups[], sessions[], presets[], selection, sidebar, windowFrame, shortcuts}`; 500 ms debounced atomic write + `.bak`; corrupt → `.bak` + notice.


---

## Roadmap

Milestones and tickets are tracked in Linear — project **tkzmux** (team Tkzmux): https://linear.app/tkzmux/project/tkzmux-4c5798217c26. The tickets are the source of truth for scope and acceptance; this document is the source of truth for the architecture.

- **M0 Foundation** — repo, SwiftPM skeleton, `make app`, this document; toolchain, fonts, theme tokens.
- **M1 Terminal surface** — vendor libghostty-vt, pty + environment, `TerminalSession` + vtdump, fonts/atlas, Metal renderer, `TerminalMetalView`, keyboard + IME, mouse/selection/clipboard, session semantics + bundle, multi-session lifecycle (must beat the cmux baseline).
- **M2 App shell & sidebar** — `TkzCore` models + `AppStore`, main window/toolbar/status-bar shell, sidebar outline view, new-session menu / ⌘P / ⇧⌘P palette / rename / groups.
- **M3 Claude integration** — `ClaudeSessionWatcher`, `tkzmux-hook` + `HookServer`, shim + ZDOTDIR wrappers + installer, status derivation, usage + per-session sidecar.
- **M4 Git & status bar** — `GitStatusService`, status-bar content + PR badge, `PortScanner`.
- **M5 Persistence & restore** — `StateFile` v1, restore flow / presets / close-remove semantics, Elsewhere group + housekeeping.
- **M6 Distribution** (TKZ-37…42 + TKZ-34) — version identity from git tags, Developer ID + hardened runtime + notarization, `make dist` + GitHub releases, `tkz0/homebrew-tap` cask, public repo + README, clean-machine install test; CI release build optional.
- **Later (sharing readiness)** — `tkzmux-statusline` wrapper, bash/fish integration, light theme + settings UI, splits; homebrew-cask core submission only if notability is ever met.

Priority in Linear: **High** = daily-driver cut line (M0, M1, M2, M3.1–M3.4, M5.1–M5.2); **Medium** = after the cut (M3.5, M4, M5.3, M6); **Low** = Later.
