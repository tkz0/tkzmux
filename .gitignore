.build/
build/
*.xcodeproj
.swiftpm/
.DS_Store
# vendor/ is committed (prebuilt libghostty-vt xcframework, M1.1)

# claude -w worktrees and the Agent tool's isolated checkouts
.claude/worktrees/

# Local-only docs: kept in the working tree, not published with the source.
# docs/perf.md is a log of numbers measured on one machine; docs/release.md is
# the personal signing/notarization runbook.
docs/perf.md
docs/release.md
